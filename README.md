# install-pip
A terminal-based Python library installer with a built-in list of 200+ packages. Supports bulk installation with a progress bar and an interactive mode with fuzzy search — mistype a name and it suggests the closest match. Clean output, zero dependencies, runs on Windows, Linux and macOS.
